version: '3.8'

services:
  resume-bot:
    build: .
    container_name: resume-bot
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    volumes:
      - /data/turing-apps/resume-bot/sessions:/app/sessions
      - /data/turing-apps/resume-bot/uploads:/app/uploads
      - /data/turing-apps/resume-bot/chroma_db:/app/chroma_db
      - /data/turing-apps/resume-bot/mineru_process:/app/mineru_process
      - /data/turing-apps/resume-bot/resume_archive:/app/resume_archive
